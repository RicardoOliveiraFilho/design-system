import { Meta } from '@storybook/addon-docs'
import { TokensGrid } from '../../components/TokensGrid'
import { fontWeights } from '@ricardo-oliveira-ignite-ui/tokens'

<Meta title="Tokens/Font Weights" />

# Font Weights

Esses são os valores para os possíveis pesos das fontes utilizados no Ignite UI.

<TokensGrid tokens={fontWeights} />